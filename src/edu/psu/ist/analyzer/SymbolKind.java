package edu.psu.ist.analyzer;

/** Tags for the kinds of definitions in a piethon script. */
public enum SymbolKind {

    /** A tag for a piethon procedure. */
    Procedure,

    /** A tag for a piethon variable. */
    Variable,

    /** A tag for a piethon formal parameter. */
    Parameter,
}
